import { FC } from 'react';
import CheckoutTemplate from '../app/Components/templates/checkout/CheckoutTemplate';

const Checkout: FC = () => {
  return <CheckoutTemplate />;
};

export default Checkout;